# k6 측정 기록

Windows 로컬 백엔드와 Docker PostgreSQL 16에서 측정했다. 모든 요청은 로그인한 테스트 계정으로 보냈고, 요청 사이에는 1초를 뒀다. 이미지 키는 `NULL`이다.

## 도감 목록: 인덱스 전후

대상: `GET /api/v1/members/me/collection-items?page=0&size=20`, 10 VU

| 도감 아이템 | 인덱스 | 시간 | 요청 수 | p95 | 실패율 |
| ---: | --- | ---: | ---: | ---: | ---: |
| 500건 | 적용 전 | 2분 | 1,163 | 70.01ms | 0% |
| 50,000건 | 적용 전 | 1분 | 549 | 136.96ms | 0% |
| 50,000건 | 적용 후 | 1분 | 590 | 27.15ms | 0% |

50,000건일 때 첫 페이지 조회는 전체 행을 읽고 정렬했다. `EXPLAIN (ANALYZE, BUFFERS)` 실행 시간은 약 15.43ms였다. `(member_id, created_at DESC)` 인덱스를 적용한 뒤에는 필요한 20행만 읽었고 약 0.07ms가 걸렸다. 공개 목록을 위한 `(member_id, is_public, created_at DESC)` 인덱스도 함께 추가했다. 두 인덱스는 Flyway V8에 넣었고, 서버를 다시 띄워 적용을 확인했다.

같은 50,000건 조건에서 HTTP p95는 136.96ms에서 27.15ms로 줄었다. 이 테스트는 10 VU가 매 요청 뒤 1초씩 쉬므로 최대 처리량 측정은 아니다.

## 다른 조회 API

각 API를 10 VU, 30초로 실행했다. 모두 실패율 0%였다.

| API | 데이터 | p95 |
| --- | --- | ---: |
| 회원 도감 목록 | 도감 아이템 50,000건 | 160.73ms |
| 도감 상세 | 아이템 1건 | 39.67ms |
| 플리마켓 목록 | 참여 마켓 100건 | 1,579.74ms |
| 플리마켓 상세 | 마켓 1건 | 544.05ms |
| 플리마켓 참여자 목록 | 참가자 1명 | 278.22ms |
| 도감 거래 상세 | 거래 요청 1건 | 1,318.47ms |

플리마켓 목록을 다시 측정했을 때 p95는 691.27ms였다. 해당 조회 SQL의 DB 실행 시간은 약 4.33ms였다. API 응답 지연의 원인은 이번 측정으로 특정하지 못했다.

## 도감 거래 상태 변경

3 VU, 30초 동안 서로 다른 아이템 81개로 요청을 만들었다. 생성·수락·완료·거절·취소 요청은 총 189건이며 모두 예상한 상태 코드로 끝났다. 전체 HTTP p95는 110.07ms, 실패율은 0%였다. DB에는 완료·거절·취소가 각각 29건 남았다.

이전 실행의 p95는 다섯 동작을 합친 값이다. 당시에는 이미지 업로드가 필요한 도감 아이템·플리마켓 생성과 수정을 측정하지 않았다. 아래 #85 측정에서 이를 추가했다.

## #85 추가 측정 (2026-09-19)

Windows 로컬 백엔드, Docker PostgreSQL 16, 로컬 MinIO에서 측정했다. 도감 아이템 50,000건과 기존 플리마켓 100건을 유지했다. 쓰기 요청의 이미지는 1픽셀 PNG다. 아래 값은 각각 10 VU, 20초 실행 결과이며, 요청 흐름 사이에는 1초를 쉬었다.

| 흐름 | 반복 | HTTP 요청 | 전체 p95 | 실패율 |
| --- | ---: | ---: | ---: | ---: |
| 도감 아이템 생성·수정·삭제 | 165 | 495 | 267.70ms | 0% |
| 플리마켓 생성·참여·수정·초대 코드 재발급·탈퇴·삭제 | 164 | 984 | 115.56ms | 0% |
| 도감 거래 생성·수락·완료·거절·취소 | 190 | 444 | 73.26ms | 0% |

| API 동작 | p95 |
| --- | ---: |
| 도감 아이템 생성 / 수정 / 삭제 | 262.61 / 340.76 / 110.96ms |
| 플리마켓 생성 / 참여 / 수정 | 188.62 / 68.63 / 209.75ms |
| 플리마켓 초대 코드 재발급 / 탈퇴 / 삭제 | 66.34 / 110.58 / 124.18ms |
| 도감 거래 생성 / 수락 / 완료 | 142.33 / 68.91 / 69.12ms |
| 도감 거래 거절 / 취소 | 64.34 / 49.94ms |

거래 흐름은 변경 전 같은 10 VU에서 10개 요청만 시작하고 완료된 반복은 0개였다. DB 연결 풀 10개가 모두 사용 중이었고, 알림 저장용 새 트랜잭션은 연결을 받지 못해 30초 후 실패했다. 거래 알림 리스너를 커밋 후 별도 작업 풀에서 실행하도록 변경한 뒤 재측정했다. 변경 후 444개 HTTP 요청이 모두 성공했고, 해당 거래 요청에 대한 알림 444건이 DB에 저장된 것을 확인했다. 알림은 이제 거래 응답 직후가 아닌 조금 뒤에 보일 수 있다. 작업 큐가 가득 차거나 프로세스가 종료되면 알림 전달 보장이 없으므로, 강한 전달 보장이 필요하면 별도 영속 큐가 필요하다.

조회 API는 각각 10 VU, 15초, 요청 간 1초 대기로 다시 측정했다. 각 API에서 150개 요청이 모두 성공했다.

| API | p95 |
| --- | ---: |
| 플리마켓 목록 | 161.49ms |
| 플리마켓 상세 | 74.06ms |
| 플리마켓 참여자 목록 | 56.56ms |
| 플리마켓 초대 코드 조회 | 23.45ms |
| 도감 거래 상세 | 99.64ms |

기존 조회 수치와 차이가 크지만 서버 재시작과 측정 환경이 달라 원인을 단정할 수 없다. 쓰기 흐름의 전후 수치도 첫 실행은 연결 풀 고갈로 실패했으므로 일반적인 최적화 비율로 해석하지 않는다.
